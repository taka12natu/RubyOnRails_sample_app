<h1>編集画面</h1>
<%= form_for(@list, url:"/todolists/#{@list.id}") do |f| %>
	<h4>タイトル</h4>
	<%= f.text_field :title %>
	<h4>本文</h4>
	<%= f.text_area :body %>
	<%= f.submit '保存' %>
<% end %>

<!-- ２行目のURLをtodolist_path[:id]でいけるかと思ったらエラー　要因は？ -->